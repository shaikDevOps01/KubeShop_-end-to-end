name: CI/CD Pipeline

on:
  push:
    branches: [ main, dev ]
  pull_request:
    branches: [ main ]

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v3
      
    - name: Set up Docker
      uses: docker/setup-buildx-action@v2
      
    - name: Install Docker Compose (Ubuntu 24.04 compatible)
      run: |
        # For Ubuntu 24.04, we need to use docker-compose (legacy) or download directly
        sudo apt-get update
        sudo apt-get install -y docker-compose
        docker-compose --version
        
    - name: Build all services
      run: |
        docker-compose build
        echo "✅ All Docker images built successfully!"
        
    - name: List built images
      run: |
        docker images
        
    - name: Validate configuration
      run: |
        docker-compose config
        echo "✅ Docker Compose configuration is valid!"
        
    - name: Verify service directories
      run: |
        echo "📁 Project structure:"
        ls -la
        echo ""
        echo "📦 Service directories found:"
        ls -d */ | grep -E "(product|cart|order|auth|frontend)"
        echo ""
        echo "✅ All validation passed!"
        
    - name: Success
      run: |
        echo "🎉 CI/CD Pipeline completed successfully!"
        echo "All microservices built and validated!"
